/* body consumer1
 * taking cpu
 * empiric control media deadline=media(deadline)*1.2
 * init sched_setattr
 * body
 * mutex-read(first dimension and then the message)-mutex
 *
 *
 *
 * */

// body consumer2 like consumer1
